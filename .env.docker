# App
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL="postgresql://vitalispulse:vitalispulse@postgres:5432/vitalispulse_db?schema=public"

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672

# Mailer
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@vitalispulse.dev
